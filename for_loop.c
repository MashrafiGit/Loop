#include <stdio.h>
int main()
{

    // for-loop - for for-loop we have for(initialize; end condition; increment,decrement)
    // for many usage we have shortcut on i=i+1 coz it's use a lot of time, so shortcut: i+=1, i++, it's also same
    for (int i = 1; i <= 5; i = i + 1)
    {
        // printf("I'm sorry\n");
        printf("%d\n", i);
    }

    return 0;
}